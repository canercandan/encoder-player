IF (OpenCV_FOUND)

MESSAGE(STATUS "include dir: ${OpenCV_INCLUDE_DIR}")
MESSAGE(STATUS "include dirs: ${OpenCV_INCLUDE_DIRS}")
MESSAGE(STATUS "lib dir: ${OpenCV_LIB_DIR}")
MESSAGE(STATUS "lib dirs: ${OpenCV_LIBRARY_DIR}")
MESSAGE(STATUS "lib dirs: ${OpenCV_LIBRARY_DIRS}")
MESSAGE(STATUS "libs: ${OpenCV_LIBS}")
MESSAGE(STATUS "libraries: ${OpenCV_LIBRARIES}")

INCLUDE_DIRECTORIES(${OpenCV_INCLUDE_DIRS})
LINK_DIRECTORIES(${OpenCV_LIBRARY_DIRS})

# SET(SOURCES
#   epitivo.cpp
#   )

# SET(LIBRARY_OUTPUT_PATH ${CMAKE_BINARY_DIR}/lib)

# ADD_LIBRARY(epitivo STATIC ${SOURCES})

ADD_EXECUTABLE(opencv openCV.cpp)

TARGET_LINK_LIBRARIES(opencv ${OpenCV_LIBRARIES})

ENDIF()
